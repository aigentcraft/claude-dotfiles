---
name: uc-planned-without-the-designed-keyword-research
title: UC「サジェスト回帰→全語の検索ボリューム→意図クラスタ→競合取得のうえで企画できているのか？」— 設計した手順を踏まずに企画していた
cluster: uc
type: "user-correction"
tags: ["user-correction", "planning", "keyword-research", "serp-cluster", "competitors", "process-skipped", "weevee"]
date: 2026-09-19
severity: high
---

## 指摘（原文）
> 回帰取得したサジェストのそれぞれすべての検索ボリュームとって同じ検索意図のクラスタ作成と、
> それぞれの競合記事の取得をしたうえで企画ができているのか？

## 答え: できていなかった
2026-09-18 に需要語 7 つ（NotebookLM 使い方 27,100 / Claude Code 料金 27,100 / Claude Code 使い方 18,100 …）を
企画したが、設計（docs/07 §6.2「サジェスト回帰 → 企画」）の 4 段階のうち、実際に踏んだのは 1 段階目の
「種の語のボリューム」だけだった。

| 設計の段階 | 実際 |
|---|---|
| サジェストを回帰取得 | **0 件**。道具（`suggest_matrix.py` / `kwcli seed-suggests`）はあるが、企画補充（02a）は呼ばない。定時にも無い |
| 全語のボリューム | 種の語 103 語のみ。サジェストが無いので派生語のボリュームも無い |
| SERP 指紋で意図クラスタ | 7 語すべて **指紋 0 件**。02a は**キャッシュを読むだけで取得しない**（`serp.load_cached`）。全 DB で 30 行 = 9/06 のさくら実験分 |
| 競合記事の取得 | 企画時点で **7 企画中 6 企画が 0 件**。競合は企画の**後**、記事に着手した時に 02c が取る。企画の構成案は競合を見ずに書かれた |

つまり seo-strategist は「語 + 月間検索数 + 自分の知識」だけで構成案を書いた。docs/07 §6.3 が要求する
「outline に競合ブロック（gap_plan）が入っていること」も満たしていない。

## なぜ起きたか
- docs/07 §6.2 は**人が道具を順に叩く手順書**として書かれ、企画補充のコードには配線されなかった
  （9/06 の実走は手作業で 1 回やっただけ）
- 02a は「SERP 指紋があれば使う」設計で、**無ければ取りに行かない**。新しい語は永久に指紋が無い
- 競合の取得は 02c（記事着手時）に置かれ、企画段階には無い。「企画に競合ブロックを必ず含める」という
  設計の要求は、02a の機械検証にも入っていなかった
- 私は 9/18 に「企画した」と報告した。**企画が設計どおりの材料で作られたかを確かめずに**、
  企画が登録されたことをもって完了とした（「作った」と「設計どおりに作った」を分けていない）

## 修正（2026-09-19 実装・実走済み）
企画補充（02a）の**前段**に材料集めを配線した — `workers/shared/keyword_research.research()`（LLM 不使用・道具の順次実行）:
種の語（未企画の需要上位 N 語）→ ①サジェスト回帰（製品名を含む語だけ候補として keywords に追記）→ ②全語の
Keyword Planner ボリューム（未取得だけ・データなしは 0 と印。kwcli の書込を `write_volume_results` に切り出して共用）
→ ③SERP 指紋（主）+ 語彙（従）で意図クラスタ（代表 = 最大ボリューム・記事にならない意図は落とす・**種の語を先に、
残りはボリューム順に取得**して上限で切られるのを尾だけにする）→ ④クラスタごとに記事級の上位 3 件 + 必須要素
（7 日以内は competitor_pages を再利用）→ `output/research/<日付>/` + ブリーフの「検索意図クラスタと競合」節。
- **③物理ゲート `has_material`**: 調査済みクラスタの語 / X シグナル / X 観測 / リライトのどれにも結ばれない提案は
  登録しない（判断は覆さない — 材料が**ある**かだけを見る）
- 登録後 `attach_to_topic`: outline に同意図の語のブロック + 競合ブロック（末尾固定 — `strip_block` はマークから
  末尾を 1 ブロックとみなすので意図ブロックはその前に置く）、competitor_pages.topic_id を結ぶ、候補語を active に昇格
- 実走: 種 7 語 → 59 語（サジェスト 52・ボリューム 40 + データなし 12）→ SERP 指紋 60 → 意図クラスタ 34 →
  競合 8 群 × 3 件。材料なしで立てた企画 52〜56 を discard し、5 語を企画し直した（60〜64: 全部に意図ブロック・
  競合ブロック・X フック・competitor_pages 3〜5 件）。執筆中の記事 27/28（企画 2/51）にも材料を結んだ
- 手で回す: `python -X utf8 tools/keyword_research.py --seeds <id…> | --auto N`。`TOPIC_RESEARCH=0` で止まる
- tests: `tests/test_keyword_research.py` 24 件（ネットワークなし・本番 schema の DB）

## 予防ルール
- **手順書は配線されるまで手順書でしかない。** 設計書に手順を書いたら、それを呼ぶコードと「踏んだか」の検証を同時に置く
- 「企画した」を報告する前に、**企画が何を材料に作られたか**を数える（サジェスト N 件・指紋 N 件・競合 N 件）
- 材料が「あれば使う」設計は、新しい対象に対して永久に材料が無い。**無ければ取りに行く**を既定にする

## 関連
- [[uc-tweet-only-contradicts-comprehensive-media]] — 前日の企画に関する指摘
- [[uc-search-volume-missing-free-seo-stack]] — 需要データの取得までは通した
- [[documented-but-never-implemented]] — 説明書に書いたことが実装されていない型
